package tools;

import java.io.IOException;
import java.io.InputStream;
import java.io.OutputStream;
import java.net.MalformedURLException;
import java.net.URL;
import java.net.URLConnection;

public class WebReporter {
	/*static private String MAGIC_KEY = "test passphrase";
	public static void main(String[] args) {
		new WebReporter();
	}
	public WebReporter() {
		try {
			URL url  = new URL("http://server.anotherprophecy.com/test/");
			URLConnection conn = url.openConnection();
			conn.setRequestProperty("key", MAGIC_KEY);
			conn.setRequestProperty("json", "{\"p\":\"test!!11\"}");
			conn.setConnectTimeout(200);
			InputStream x = conn.getInputStream();
			int v;
			while((v = x.read()) != -1) {
				//System.out.println(v);
			}

		} catch (MalformedURLException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		};
	}*/
	

}
